[plugin]
name = "maze"
version = "0.2.0"
description = "Terminal maze generator and solver with Unicode rendering"
author = "corvid-agent"

[[commands]]
name = "maze"
description = "Generate and solve terminal mazes"
binary = "bin/maze"

[capabilities]
exec = false
store = false
metadata = false
